# TCK Pass-Rate Report — T+2:30

**Board item:** T-0002 (openCypher TCK harness wire-up)
**Rubric:** Cat. 4 (openCypher/TCK — GATE, weight 12)
**Timestamp:** 2026-06-13T20:54:00Z (T0+2:30)
**Harness:** `tck-runner` v0.0.0 (crate at `tck-runner/`)

## Live Result (stub engine — no Cypher impl yet)

```
openCypher TCK results (corpus: tck/openCypher/features)
  pinned tag:   2024.3
  total:        1602
  pass:         0
  pending:      1602
  fail:         0
  parse_errors: 1
  pass_rate:    0.0000 (0.00%)
```

## JSON (machine-readable for grader)

```json
{
  "total": 1602,
  "pass": 0,
  "pending": 1602,
  "fail": 0,
  "parse_errors": 1,
  "pass_rate": 0.0,
  "tck_tag": "2024.3",
  "pinned_commit": "677cbafabb8c3c5eed458fd3b1ec0daec8d67d23"
}
```

## Interpretation

- **pass_rate = 0.000000** (0 / 1602). This is the Cat. 4 GATE score until EPIC-002 lands.
- **0 fail** — no regression; the engine simply does not implement Cypher yet.
- **1602 pending** — all scenarios ran through `PendingEngine`, which returns
  `Unsupported` for every construct. Correct behaviour for the current milestone.
- **1 parse_error** — `Literals6.feature` (13 scenarios) is known-unparseable by
  `gherkin 0.16` (the parse gap owned by **BUG-0018**; this report originally
  mis-cited "BUG-0008", which is an unrelated SPDX license bug — corrected per
  BUG-0018). These 13 scenarios land in `parse_errors`, not `pending`, so they
  cannot inflate the rate. Named and guarded in
  `tests/vendored_corpus.rs::parse_gap_is_exactly_literals6`.
- **Denominator gap (BUG-0009):** `Scenario Outline` blocks are counted once, not
  per `Examples` row. The conventional expanded total at tag `2024.3` is ~3880.
  The harness pins the unexpanded count (1602) explicitly — the choice is honest,
  not silent. Decision 0013.

## Suite integrity

| Check | Value |
|---|---|
| Vendored tag | `2024.3` |
| Vendored commit | `677cbafabb8c3c5eed458fd3b1ec0daec8d67d23` |
| Feature files | 220 |
| Parseable scenarios (unexpanded outlines) | 1602 |
| Known-unparseable scenarios (Literals6) | 13 |
| Official total (as per Decision 0008 pin for M23) | 1615 |

Note (point-in-time, T+2:30): Decision 0008 pinned `1.0.0-M23` with 1615
scenarios while the harness vendored `2024.3`. This report originally described
`2024.3` as "the same corpus renamed" — that conflation was wrong (they are
distinct openCypher releases) and was one of the discrepancies BUG-0018
surfaced. **Reconciled by Decision 0034 (T+4:30):** the vendored `2024.3` /
`677cbaf` corpus is canonical and the `caerostris_db::tck` spec pin was updated to
match (expanded `total = 3884`). The 1602/1615 figures here are the pre-BUG-0009
(unexpanded) counts as observed at T+2:30 and are retained for historical
accuracy; the current live denominator is 3884 (BUG-0009 outline expansion).

## Status

The Cat. 4 harness is NOW WIRED. The grader can read a live pass-rate number.
The score will rise from 0.00% as EPIC-002 query engine features land. The
denominator is non-gameable per Decision 0008 (`pass / total` with `pending` in
denominator). No scenario is excluded.

Landing commit: to be filled by integrator.
