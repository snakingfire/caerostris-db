# License manifest — caerostris-db
#
# The committed record of every third-party dependency and the license under
# which we use it. Rubric Cat. 12 (process health) requires this manifest to be
# complete and every license to be permissive (see
# docs/process/open-source-guardrails.md, section 5).
#
# This file is checked automatically:
#   - `tests/license_manifest.rs` parses Cargo.lock and fails CI if any
#     dependency here is missing or carries a non-permissive SPDX id.
#   - `cargo deny check licenses` (deny.toml) cross-checks against crate metadata.
#
# When you add a dependency to Cargo.toml:
#   1. Run `cargo license --avoid-build-deps` (or read the crate's `license`
#      field) to find its SPDX identifier.
#   2. Confirm the SPDX id is in the approved allow-list
#      (see `APPROVED_SPDX` in src/licenses.rs / open-source-guardrails.md).
#      If it is copyleft/source-available, get steering sign-off first and
#      record the decision in .project/decisions/ BEFORE adding it.
#   3. Add a `[[dependency]]` block below with name, version, spdx, and a note.
#
# Format (parsed by src/licenses.rs — keep keys on their own lines):
#
#   [[dependency]]
#   name = "<crate name as in Cargo.lock>"
#   version = "<resolved version>"
#   spdx = "<SPDX expression, e.g. MIT OR Apache-2.0>"
#   note = "<one line: why it is permissive / why we use it>"
#
# Dependencies introduced by the tck-runner crate (T-0002).
# All are standard permissive crates widely used in the Rust ecosystem.

[[dependency]]
name = "gherkin"
version = "0.16.0"
spdx = "MIT OR Apache-2.0"
note = "Gherkin (BDD) feature-file parser; dual-licensed permissive."

[[dependency]]
name = "heck"
version = "0.5.0"
spdx = "MIT OR Apache-2.0"
note = "Case-conversion utilities; dual-licensed permissive."

[[dependency]]
name = "itoa"
version = "1.0.18"
spdx = "MIT OR Apache-2.0"
note = "Fast integer-to-string conversion; dual-licensed permissive."

[[dependency]]
name = "memchr"
version = "2.8.2"
spdx = "Unlicense OR MIT"
note = "Fast byte-search primitives; dual-licensed permissive."

[[dependency]]
name = "peg"
version = "0.6.3"
spdx = "MIT"
note = "PEG parser macro; MIT licensed."

[[dependency]]
name = "peg-macros"
version = "0.6.3"
spdx = "MIT"
note = "Procedural macro component of peg; MIT licensed."

[[dependency]]
name = "peg-runtime"
version = "0.6.3"
spdx = "MIT"
note = "Runtime component of peg; MIT licensed."

[[dependency]]
name = "proc-macro2"
version = "1.0.106"
spdx = "MIT OR Apache-2.0"
note = "Proc-macro token stream; dual-licensed permissive."

[[dependency]]
name = "quote"
version = "1.0.45"
spdx = "MIT OR Apache-2.0"
note = "Quasi-quoting macro helper; dual-licensed permissive."

[[dependency]]
name = "serde"
version = "1.0.228"
spdx = "MIT OR Apache-2.0"
note = "Serialization/deserialization framework; dual-licensed permissive."

[[dependency]]
name = "serde_core"
version = "1.0.228"
spdx = "MIT OR Apache-2.0"
note = "Core serde traits; dual-licensed permissive."

[[dependency]]
name = "serde_derive"
version = "1.0.228"
spdx = "MIT OR Apache-2.0"
note = "Derive macros for serde; dual-licensed permissive."

[[dependency]]
name = "serde_json"
version = "1.0.150"
spdx = "MIT OR Apache-2.0"
note = "JSON serialization; dual-licensed permissive."

[[dependency]]
name = "smawk"
version = "0.3.2"
spdx = "MIT"
note = "SMAWK algorithm for matrix searching; MIT licensed."

[[dependency]]
name = "syn"
version = "2.0.117"
spdx = "MIT OR Apache-2.0"
note = "Rust syntax parsing for proc macros; dual-licensed permissive."

[[dependency]]
name = "tck-runner"
version = "0.0.0"
spdx = "MIT"
note = "Internal crate: openCypher TCK conformance runner; MIT licensed."

[[dependency]]
name = "textwrap"
version = "0.16.2"
spdx = "MIT"
note = "Text wrapping utilities; MIT licensed."

[[dependency]]
name = "thiserror"
version = "2.0.18"
spdx = "MIT OR Apache-2.0"
note = "Error derive macro; dual-licensed permissive."

[[dependency]]
name = "thiserror-impl"
version = "2.0.18"
spdx = "MIT OR Apache-2.0"
note = "Proc-macro impl for thiserror; dual-licensed permissive."

[[dependency]]
name = "typed-builder"
version = "0.23.2"
spdx = "MIT OR Apache-2.0"
note = "Compile-time checked builder pattern; dual-licensed permissive."

[[dependency]]
name = "typed-builder-macro"
version = "0.23.2"
spdx = "MIT OR Apache-2.0"
note = "Proc-macro component of typed-builder; dual-licensed permissive."

[[dependency]]
name = "unicode-ident"
version = "1.0.24"
spdx = "MIT OR Apache-2.0"
note = "Unicode identifier tables for proc-macro2; permissive (MIT/Apache-2.0 component chosen)."

[[dependency]]
name = "unicode-linebreak"
version = "0.1.5"
spdx = "Apache-2.0"
note = "Unicode line-break algorithm; Apache-2.0 licensed."

[[dependency]]
name = "unicode-width"
version = "0.2.2"
spdx = "MIT OR Apache-2.0"
note = "Unicode character width; dual-licensed permissive."

[[dependency]]
name = "zmij"
version = "1.0.21"
spdx = "MIT"
note = "Dependency of gherkin (builder macro); MIT licensed."

# ---------------------------------------------------------------------------
# Dependencies introduced by the `python/` PyO3 bindings crate (T-0030,
# EPIC-007). That crate is its own isolated workspace (own python/Cargo.lock),
# so these are recorded here and audited by tests/license_manifest.rs against
# python/Cargo.lock. All are standard, permissive crates in the PyO3 ecosystem.
# (heck, proc-macro2, quote, syn, unicode-ident are already recorded above and
# shared with the engine workspace.)
# ---------------------------------------------------------------------------

[[dependency]]
name = "pyo3"
version = "0.27.2"
spdx = "MIT OR Apache-2.0"
note = "Rust<->Python FFI layer (the bindings substrate); dual-licensed permissive."

[[dependency]]
name = "pyo3-ffi"
version = "0.27.2"
spdx = "MIT OR Apache-2.0"
note = "Raw CPython FFI bindings used by pyo3; dual-licensed permissive."

[[dependency]]
name = "pyo3-build-config"
version = "0.27.2"
spdx = "MIT OR Apache-2.0"
note = "Build-time interpreter discovery for pyo3; dual-licensed permissive."

[[dependency]]
name = "pyo3-macros"
version = "0.27.2"
spdx = "MIT OR Apache-2.0"
note = "Proc-macro front-end (#[pymodule]/#[pyfunction]) for pyo3; dual-licensed permissive."

[[dependency]]
name = "pyo3-macros-backend"
version = "0.27.2"
spdx = "MIT OR Apache-2.0"
note = "Code-generation backend for pyo3-macros; dual-licensed permissive."

[[dependency]]
name = "libc"
version = "0.2.186"
spdx = "MIT OR Apache-2.0"
note = "Raw libc/CPython symbol declarations used by pyo3-ffi; dual-licensed permissive."

[[dependency]]
name = "indoc"
version = "2.0.7"
spdx = "MIT OR Apache-2.0"
note = "Indented-string macro used by pyo3's generated code; dual-licensed permissive."

[[dependency]]
name = "unindent"
version = "0.2.4"
spdx = "MIT OR Apache-2.0"
note = "Companion to indoc (pyo3 code-gen); dual-licensed permissive."

[[dependency]]
name = "memoffset"
version = "0.9.1"
spdx = "MIT"
note = "offset_of! for pyo3-ffi struct layout; MIT licensed."

[[dependency]]
name = "once_cell"
version = "1.21.4"
spdx = "MIT OR Apache-2.0"
note = "Lazy one-time initialization used by pyo3; dual-licensed permissive."

[[dependency]]
name = "autocfg"
version = "1.5.1"
spdx = "Apache-2.0 OR MIT"
note = "Build-time rustc feature probing (memoffset build dep); dual-licensed permissive."

[[dependency]]
name = "portable-atomic"
version = "1.13.1"
spdx = "Apache-2.0 OR MIT"
note = "Portable atomics shim used by pyo3 on some targets; dual-licensed permissive."

[[dependency]]
name = "rustversion"
version = "1.0.22"
spdx = "MIT OR Apache-2.0"
note = "Compile-time rustc-version cfg attributes used by pyo3; dual-licensed permissive."

[[dependency]]
name = "target-lexicon"
version = "0.13.5"
spdx = "Apache-2.0"
note = "Target-triple parsing (pyo3-build-config). Upstream SPDX is `Apache-2.0 WITH LLVM-exception`; the LLVM exception only *relaxes* Apache-2.0's terms (so it is at least as permissive), recorded here as the base Apache-2.0 — see open-source-guardrails.md §5."
